We should create a wizard during the installation that allows the samples to be deployed with a checkbox against each one.  There should be a warning on this that existing samples of the same name would be overridden.

We should add a button press sound effect

We should update the elite dangerous menu so that it has a power presets sub-menu, ensuring we can send multiple keys to an application.

We should update the config guide with references and examples of powershell and dynamic submenus.

We should update how release notes work

We should update the Readme.md with mention of the samples and the wizard in the installer.

Provide a way of minimizing the window

Provide a way of pinning the current profile
