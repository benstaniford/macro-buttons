We should create a config editor that let's you visually see the table and then edit it's settings in a different menu

<task>

<highlevel>Add a config editor to the tray menu</highlevel>

<appearance>The editor should show two panes.  On the left should be the grid layout of the current config.  It should be possible to add or delete columns or rows by right clicking.  On the right should be a screen which allows the action, title and tile theme to be edited.</appearance>

<additional>The old "Edit config" optional should be renamed to "Edit config JSON" and the config editor should be accessed via "Edit config"</additional>

</task>

We should update the elite dangerous menu so that it has a power presets sub-menu, ensuring we can send multiple keys to an application.

We should update the config guide with references and examples of powershell and dynamic submenus.

We should update how release notes work

We should update the Readme.md with mention of the samples and the wizard in the installer.

Provide a way of minimizing the window

Provide a way of pinning the current profile

We should give a visual animation of buttons being pressed
