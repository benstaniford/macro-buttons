<Window x:Class="MacroButtons.Views.ThemeEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit Themes"
        Width="700"
        Height="500"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">

    <Window.Resources>
        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#F1F1F1"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- Label Style -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#F1F1F1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5,0,2"/>
        </Style>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#333337"/>
            <Setter Property="Foreground" Value="#F1F1F1"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title and Add Button -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Themes" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="+ Add Theme" Command="{Binding AddThemeCommand}" HorizontalAlignment="Right"/>
        </Grid>

        <!-- Themes List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Themes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#3E3E42" BorderThickness="1" Margin="0,0,0,10" Padding="10" Background="#2D2D30">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Theme Name -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3"
                                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="Theme name (e.g., 'default', 'prominent', 'utility')"/>

                                <!-- Foreground Color -->
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Foreground:" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="1" Grid.Column="1"
                                         Text="{Binding Foreground, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="Named color or hex code (e.g., 'cyan', '#00FFFF')"/>
                                <Border Grid.Row="1" Grid.Column="2" Width="40" Height="25" Margin="5,0"
                                        Background="{Binding ForegroundBrush}"
                                        BorderBrush="#3E3E42" BorderThickness="1"/>
                                <Button Grid.Row="1" Grid.Column="3" Content="Pick..."
                                        Command="{Binding PickForegroundColorCommand}"
                                        Padding="8,4" Margin="0"/>

                                <!-- Background Color -->
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Background:" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="2" Grid.Column="1"
                                         Text="{Binding Background, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="Named color or hex code (e.g., 'black', '#000000')"/>
                                <Border Grid.Row="2" Grid.Column="2" Width="40" Height="25" Margin="5,0"
                                        Background="{Binding BackgroundBrush}"
                                        BorderBrush="#3E3E42" BorderThickness="1"/>
                                <Button Grid.Row="2" Grid.Column="3" Content="Pick..."
                                        Command="{Binding PickBackgroundColorCommand}"
                                        Padding="8,4" Margin="0"/>

                                <!-- Style -->
                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Style:" VerticalAlignment="Center"/>
                                <ComboBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3"
                                          SelectedItem="{Binding Style, UpdateSourceTrigger=PropertyChanged}"
                                          ToolTip="Visual style: retro (monospace, square) or modern (sans-serif, rounded)">
                                    <ComboBoxItem Content="retro"/>
                                    <ComboBoxItem Content="modern"/>
                                    <ComboBox.ItemContainerStyle>
                                        <Style TargetType="ComboBoxItem">
                                            <Setter Property="Background" Value="#2D2D30"/>
                                            <Setter Property="Foreground" Value="#F1F1F1"/>
                                        </Style>
                                    </ComboBox.ItemContainerStyle>
                                </ComboBox>

                                <!-- Delete Button -->
                                <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="4"
                                        Content="Delete This Theme"
                                        Command="{Binding DataContext.DeleteThemeCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Background="#C42B1C"
                                        Margin="0,10,0,0"
                                        HorizontalAlignment="Right"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Command="{Binding OkCommand}" Width="80"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
